{
    "name": "aviran/cyron-framework",
    "description": "Cyron PHP framework and application foundation",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.2",
        "ext-mysqli": "*",
        "ext-json": "*",
        "ext-mbstring": "*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Cyron\\": "src/Cyron/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "@php -r \"foreach (glob('tests/*.php') as $test) { if (basename($test) === 'insert-test-book.php') continue; passthru(PHP_BINARY . ' ' . escapeshellarg($test), $code); if ($code !== 0) exit($code); }\"",
        "validate-project": "@composer validate --no-check-publish"
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
