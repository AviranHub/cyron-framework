@extends('admin.layout')
@section('content')
<div dir="rtl" class="space-y-5"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-800">مدیریت موضوع‌های انجمن</h1><p class="text-sm text-slate-500">قفل، سنجاق یا حذف موضوع‌های نامناسب</p></div><a class="text-sm text-indigo-600" href="{{ route('admin.forum.index') }}">بازگشت</a></div><div class="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-right text-xs text-slate-500">موضوع</th><th class="px-4 py-3 text-right text-xs text-slate-500">دسته</th><th class="px-4 py-3 text-right text-xs text-slate-500">وضعیت</th><th class="px-4 py-3 text-right text-xs text-slate-500">عملیات</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($topics as $topic)<tr><td class="px-4 py-3"><strong>{{ $topic->title }}</strong><small class="block text-slate-400">{{ $topic->created_at }}</small></td><td class="px-4 py-3 text-sm">{{ $categories[(int) $topic->category_id]->name ?? '-' }}</td><td class="px-4 py-3 text-sm">{{ $topic->is_locked ? 'قفل' : 'باز' }} · {{ $topic->is_pinned ? 'سنجاق‌شده' : 'عادی' }}</td><td class="px-4 py-3"><div class="flex gap-2"><form method="POST" action="{{ route('admin.forum.topics.toggle-lock', ['id' => $topic->id]) }}">@csrf<button class="text-sm text-amber-700">{{ $topic->is_locked ? 'بازکردن' : 'قفل' }}</button></form><form method="POST" action="{{ route('admin.forum.topics.toggle-pin', ['id' => $topic->id]) }}">@csrf<button class="text-sm text-indigo-700">{{ $topic->is_pinned ? 'برداشتن سنجاق' : 'سنجاق' }}</button></form><form method="POST" action="{{ route('admin.forum.topics.destroy', ['id' => $topic->id]) }}">@csrf<input type="hidden" name="_method" value="DELETE"><button class="text-sm text-red-700" type="submit">حذف</button></form></div></td></tr>@endforeach</tbody></table></div><div class="p-4">{!! $topics->links() !!}</div></div>
@endsection