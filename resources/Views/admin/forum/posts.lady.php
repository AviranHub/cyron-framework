@extends('admin.layout')
@section('content')
<div dir="rtl" class="space-y-5"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold text-slate-800">مدیریت پاسخ‌های انجمن</h1><p class="text-sm text-slate-500">بررسی و حذف پاسخ‌های گزارش‌شده یا نامناسب</p></div><a class="text-sm text-indigo-600" href="{{ route('admin.forum.index') }}">بازگشت</a></div><div class="space-y-3">@if(count($posts->items())) @foreach($posts as $post)<article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="mb-3 flex items-center justify-between"><span class="text-xs text-slate-400">{{ $post->created_at }} · {{ $users[(int) $post->user_id]->name ?? 'کاربر' }}</span><form method="POST" action="{{ route('admin.forum.posts.destroy', ['id' => $post->id]) }}">@csrf<input type="hidden" name="_method" value="DELETE"><button class="text-sm text-red-700" type="submit">حذف پاسخ</button></form></div><p class="text-sm leading-7 text-slate-700">{{ $post->content }}</p><small class="mt-2 block text-slate-400">موضوع: {{ $topics[(int) $post->topic_id]->title ?? '-' }}</small></article>@endforeach @else<div class="rounded-xl border border-slate-200 bg-white p-5 text-sm text-slate-500">پاسخی ثبت نشده است.</div>@endif</div><div class="p-4">{!! $posts->links() !!}</div></div>
@endsection