@extends('admin.layout')
@section('content')
<div dir="rtl" class="space-y-5">
    <div class="flex items-center justify-between"><div><span class="text-xs font-semibold text-indigo-600">COMMUNITY</span><h1 class="text-2xl font-bold text-slate-800">مدیریت انجمن گفتگو</h1><p class="text-sm text-slate-500">کنترل دسته‌بندی‌ها، موضوع‌ها و پاسخ‌های کاربران</p></div><a class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white" href="{{ route('admin.forum.topics') }}">همه موضوع‌ها</a></div>
    <div class="grid gap-4 md:grid-cols-3"><a class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('admin.forum.topics') }}"><span class="text-sm text-slate-500">موضوع‌ها</span><strong class="mt-2 block text-2xl text-slate-800">{{ $topics->count() }}</strong></a><a class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('admin.forum.posts') }}"><span class="text-sm text-slate-500">پاسخ‌های اخیر</span><strong class="mt-2 block text-2xl text-slate-800">{{ $posts->count() }}</strong></a><a class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('admin.forum_categories.index') }}"><span class="text-sm text-slate-500">دسته‌بندی‌ها</span><strong class="mt-2 block text-2xl text-slate-800">{{ $categories->count() }}</strong></a></div>
    <section class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div class="mb-4 flex items-center justify-between"><h2 class="font-bold text-slate-800">موضوع‌های اخیر</h2><a class="text-sm text-indigo-600" href="{{ route('admin.forum.topics') }}">مشاهده همه</a></div><div class="space-y-3">@if($topics->count()) @foreach($topics as $topic)<div class="flex items-center justify-between border-b border-slate-100 pb-3"><div><strong>{{ $topic->title }}</strong><small class="block text-slate-400">{{ $topic->created_at }}</small></div><span class="text-sm text-slate-500">{{ $topic->views }} بازدید</span></div>@endforeach @else<div class="text-sm text-slate-500">هنوز موضوعی ثبت نشده است.</div>@endif</div></section>
</div>
@endsection