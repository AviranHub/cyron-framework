@extends('admin.layout')
@section('content')
<div dir="rtl" class="space-y-5">
    <div class="flex flex-wrap items-center justify-between gap-3"><div><span class="text-xs font-semibold text-indigo-600">ACTIVITY CENTER</span><h1 class="text-2xl font-bold text-slate-800">فعالیت کاربران</h1><p class="text-sm text-slate-500">رویدادهای ورود، مطالعه، خرید، پشتیبانی و تعاملات کاربران.</p></div><div class="flex gap-3"><a href="{{ route('admin.activities.export') }}?{{ http_build_query($_GET) }}" class="text-sm text-indigo-600">خروجی CSV</a><a href="{{ route('admin.dashboard') }}" class="text-sm text-indigo-600"><i class="fas fa-arrow-right ml-1"></i>داشبورد</a></div></div>
    <form method="GET" class="grid gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-5"><select name="user_id" class="rounded-lg border-slate-300"><option value="">همه کاربران</option>@foreach($users as $user)<option value="{{ $user->id }}" @if((string) input('user_id') === (string) $user->id) selected @endif>{{ $user->name }} - {{ $user->email }}</option>@endforeach</select><input name="category" value="{{ input('category') }}" class="rounded-lg border-slate-300" placeholder="دسته‌بندی"><input name="action" value="{{ input('action') }}" class="rounded-lg border-slate-300" placeholder="رویداد مثل book.viewed"><input type="date" name="from" value="{{ input('from') }}" class="rounded-lg border-slate-300"><button class="rounded-lg bg-indigo-600 px-4 py-2 font-semibold text-white hover:bg-indigo-700" type="submit"><i class="fas fa-filter ml-1"></i>فیلتر</button></form>
    <div class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-right text-xs text-slate-500">کاربر</th><th class="px-4 py-3 text-right text-xs text-slate-500">رویداد</th><th class="px-4 py-3 text-right text-xs text-slate-500">دسته</th><th class="px-4 py-3 text-right text-xs text-slate-500">موضوع</th><th class="px-4 py-3 text-right text-xs text-slate-500">زمان</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($activityRows as $row)<tr class="hover:bg-slate-50"><td class="px-4 py-3 text-sm"><a class="font-semibold text-indigo-700" href="{{ $row['user'] ? route('admin.activities.user', ['id' => $row['user']->id]) : '#' }}">{{ $row['user']->name ?? 'مهمان/نامشخص' }}</a><small class="block text-slate-400">{{ $row['user']->email ?? ($row['activity']->user_id ? 'شناسه: ' . $row['activity']->user_id : '') }}</small></td><td class="px-4 py-3 text-sm font-medium text-slate-700"><i class="fas fa-bolt ml-1 text-amber-500"></i>{{ $row['activity']->action }}</td><td class="px-4 py-3 text-sm text-slate-500">{{ $row['activity']->category }}</td><td class="px-4 py-3 text-sm text-slate-500">{{ $row['activity']->subject_type ? basename(str_replace('\\', '/', $row['activity']->subject_type)) . ' #' . $row['activity']->subject_id : '-' }}</td><td class="px-4 py-3 text-xs text-slate-400">{{ $row['activity']->occurred_at }}</td></tr>@endforeach</tbody></table></div><div class="p-4">{!! $activities->links() !!}</div></div>
</div>
@endsection