@extends('author.layouts.app')
@section('content')
<div class="space-y-6"><section class="flex flex-wrap items-end justify-between gap-4"><div><span class="text-xs font-extrabold tracking-[.18em] text-emerald-600">YOUR CATALOG</span><h1 class="mt-2 text-3xl font-black text-emerald-950 dark:text-white">آثار من</h1><p class="mt-2 text-slate-500">فهرست کتاب‌هایی که در کلبه کتاب می‌سازی.</p></div><a class="inline-flex min-h-11 items-center gap-2 rounded-xl bg-emerald-600 px-5 font-bold text-white transition hover:bg-emerald-700" href="{{ route('author.books.create') }}"><i class="fas fa-plus"></i> افزودن اثر</a></section>@if(session('success'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-emerald-800">{{ session('success') }}</div>@endif@if(session('error'))<div class="rounded-xl border border-rose-200 bg-rose-50 p-4 text-rose-800">{{ session('error') }}</div>@endif<form method="GET" class="grid gap-3 rounded-2xl border border-emerald-100 bg-white p-4 shadow-sm sm:grid-cols-[1fr_180px_auto]"><input name="search" value="{{ $search }}" placeholder="جستجوی عنوان یا نویسنده" class="min-h-11 rounded-xl border border-slate-200 px-3"><select name="status" class="min-h-11 rounded-xl border border-slate-200 px-3"><option value="">همه وضعیت‌ها</option><option value="draft" @if($status === 'draft') selected @endif>پیش‌نویس</option><option value="published" @if($status === 'published') selected @endif>منتشرشده</option></select><button class="min-h-11 rounded-xl bg-cyan-700 px-5 font-bold text-white" type="submit">فیلتر</button></form><section class="overflow-hidden rounded-2xl border border-emerald-100 bg-white shadow-sm">@if(count($books->items()))<div class="divide-y divide-slate-100">@foreach($books as $book)<div class="flex flex-wrap items-center gap-4 p-4 sm:p-5"><span class="grid h-12 w-12 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><i class="fas fa-book"></i></span><span class="min-w-0 flex-1"><strong class="block truncate">{{ $book->title }}</strong><small class="text-slate-500">{{ $book->author_name ?? 'نویسنده ثبت نشده' }} · {{ $book->status ?? 'پیش‌نویس' }}</small></span><a class="rounded-lg bg-cyan-50 px-3 py-2 text-sm font-bold text-cyan-800" href="{{ route('author.books.parts.create', ['id' => $book->id]) }}">صفحات</a><a class="rounded-lg bg-emerald-50 px-3 py-2 text-sm font-bold text-emerald-800" href="{{ route('author.books.edit', ['id' => $book->id]) }}">ویرایش</a>@if(($book->status ?? 'draft') === 'published')<form method="POST" action="{{ route('author.books.unpublish', ['id' => $book->id]) }}">@csrf<button class="rounded-lg bg-amber-50 px-3 py-2 text-sm font-bold text-amber-800" type="submit">پیش‌نویس</button></form>@else<form method="POST" action="{{ route('author.books.publish', ['id' => $book->id]) }}">@csrf<button class="rounded-lg bg-emerald-600 px-3 py-2 text-sm font-bold text-white" type="submit">انتشار</button></form>@endif</div>@endforeach</div>@else<div class="p-12 text-center text-slate-500"><i class="fas fa-box-open text-3xl text-emerald-600"></i><p class="mt-3">اثری برای نمایش وجود ندارد.</p></div>@endif</section><div>{!! $books->links() !!}</div></div>
+@endsection
