@extends('author.layouts.app')
@section('content')
<div class="mx-auto max-w-4xl space-y-6"><section><span class="text-xs font-extrabold tracking-[.18em] text-emerald-600">NEW WORK</span><h1 class="mt-2 text-3xl font-black text-emerald-950 dark:text-white">ساخت کتاب جدید</h1><p class="mt-2 text-slate-500">کتاب ابتدا به‌صورت پیش‌نویس ذخیره می‌شود و بعد می‌توانی صفحاتش را اضافه کنی.</p></section>@if(session('error'))<div class="rounded-xl border border-rose-200 bg-rose-50 p-4 text-rose-800">{{ session('error') }}</div>@endif<form method="POST" action="{{ route('author.books.store') }}" enctype="multipart/form-data" class="grid gap-5 rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm sm:p-7">@csrf<label class="font-bold">عنوان کتاب<input name="title" required maxlength="191" value="{{ old('title') }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal outline-none focus:border-emerald-500"></label><div class="grid gap-5 sm:grid-cols-2"><label class="font-bold">جلد کتاب<input name="cover" type="file" accept="image/jpeg,image/png,image/webp" required class="mt-2 block w-full rounded-xl border border-dashed border-emerald-300 bg-emerald-50 p-3 font-normal"></label><label class="font-bold">قیمت به تومان<input name="price" type="number" min="0" step="1000" value="{{ old('price', 0) }}" required class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"></label></div><label class="font-bold">دسته‌بندی<select name="category_id" class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"><option value="">بدون دسته‌بندی</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></label><label class="font-bold">لینک PDF اختیاری<input name="pdf" type="url" value="{{ old('pdf') }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"></label><label class="font-bold">معرفی کوتاه<textarea name="introduction" rows="3" class="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal">{{ old('introduction') }}</textarea></label><label class="font-bold">توضیحات<textarea name="description" rows="6" class="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal">{{ old('description') }}</textarea></label><button class="min-h-12 rounded-xl bg-emerald-600 px-5 font-bold text-white transition hover:bg-emerald-700" type="submit"><i class="fas fa-book-medical ml-2"></i> ساخت پیش‌نویس کتاب</button></form></div>
+@endsection
