@extends('author.layouts.app')
@section('content')
<div class="mx-auto max-w-4xl space-y-6"><section><span class="text-xs font-extrabold tracking-[.18em] text-emerald-600">EDIT WORK</span><h1 class="mt-2 text-3xl font-black text-emerald-950 dark:text-white">ویرایش {{ $book->title }}</h1><p class="mt-2 text-slate-500">اطلاعات کتاب را اصلاح کن؛ صفحات و انتشار آن جداگانه مدیریت می‌شوند.</p></section>@if(session('success'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-emerald-800">{{ session('success') }}</div>@endif@if(session('error'))<div class="rounded-xl border border-rose-200 bg-rose-50 p-4 text-rose-800">{{ session('error') }}</div>@endif<form method="POST" action="{{ route('author.books.update', ['id' => $book->id]) }}" enctype="multipart/form-data" class="grid gap-5 rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm sm:p-7">@csrf @method('PUT')<label class="font-bold">عنوان کتاب<input name="title" required maxlength="191" value="{{ old('title', $book->title) }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"></label><div class="grid gap-5 sm:grid-cols-2"><label class="font-bold">جلد جدید اختیاری<input name="cover" type="file" accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full rounded-xl border border-dashed border-emerald-300 bg-emerald-50 p-3 font-normal"></label><label class="font-bold">قیمت به تومان<input name="price" type="number" min="0" step="1000" value="{{ old('price', $book->price) }}" required class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"></label></div><label class="font-bold">دسته‌بندی<select name="category_id" class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"><option value="">بدون دسته‌بندی</option>@foreach($categories as $category)<option value="{{ $category->id }}" @if((string) old('category_id', $book->category_id) === (string) $category->id) selected @endif>{{ $category->name }}</option>@endforeach</select></label><label class="font-bold">لینک PDF اختیاری<input name="pdf" type="url" value="{{ old('pdf', $book->pdf) }}" class="mt-2 min-h-11 w-full rounded-xl border border-slate-200 px-3 font-normal"></label><label class="font-bold">معرفی کوتاه<textarea name="introduction" rows="3" class="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal">{{ old('introduction', $book->introduction) }}</textarea></label><label class="font-bold">توضیحات<textarea name="description" rows="6" class="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal">{{ old('description', $book->description) }}</textarea></label><div class="flex flex-wrap gap-3"><button class="min-h-11 rounded-xl bg-emerald-600 px-5 font-bold text-white transition hover:bg-emerald-700" type="submit"><i class="fas fa-save ml-2"></i> ذخیره تغییرات</button><a class="inline-flex min-h-11 items-center rounded-xl bg-cyan-50 px-5 font-bold text-cyan-800" href="{{ route('author.books.parts.create', ['id' => $book->id]) }}"><i class="fas fa-file-lines ml-2"></i> مدیریت صفحات</a></div></form><div class="flex flex-wrap items-center gap-3 rounded-2xl border border-amber-100 bg-amber-50 p-5"><span class="font-bold text-amber-950">وضعیت فعلی: {{ ($book->status ?? 'draft') === 'published' ? 'منتشرشده' : 'پیش‌نویس' }}</span>@if(($book->status ?? 'draft') === 'published')<form method="POST" action="{{ route('author.books.unpublish', ['id' => $book->id]) }}">@csrf<button class="rounded-xl bg-amber-700 px-4 py-2 font-bold text-white" type="submit">بازگردانی به پیش‌نویس</button></form>@else<form method="POST" action="{{ route('author.books.publish', ['id' => $book->id]) }}">@csrf<button class="rounded-xl bg-emerald-700 px-4 py-2 font-bold text-white" type="submit">انتشار کتاب</button></form>@endif</div></div>
+@endsection
