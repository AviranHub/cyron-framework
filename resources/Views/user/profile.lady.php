@extends('layouts.master')

@section('content')
<div class="container mx-auto px-4 py-10"><section class="bg-white dark:bg-zinc-900 rounded-xl shadow p-6 flex flex-col md:flex-row gap-5 items-center"><div class="w-20 h-20 rounded-full bg-green-100 text-green-700 flex items-center justify-center text-3xl"><i class="fas fa-user"></i></div><div class="text-center md:text-right"><span class="text-sm text-gray-500">پروفایل عمومی</span><h1 class="text-3xl font-bold">{{ $user->name }}</h1><p class="text-gray-500">@{{ $user->username }}</p>@if($user->bio)<p class="mt-2">{{ $user->bio }}</p>@endif</div></section><section class="mt-8"><h2 class="text-2xl font-bold mb-5">کتاب‌های منتشرشده</h2>@if(empty($books))<p class="text-gray-500">هنوز کتاب منتشرشده‌ای برای این کاربر ثبت نشده است.</p>@else<div class="grid grid-cols-2 md:grid-cols-4 gap-5">@foreach($books as $book)<a href="{{ route('book', ['slug' => $book->slug]) }}" class="bg-white dark:bg-zinc-900 rounded-lg shadow p-3"><img class="w-full h-48 object-cover rounded" src="{{ storage_url('/book/' . $book->cover) }}" alt="جلد {{ $book->title }}"><h3 class="font-bold mt-3">{{ $book->title }}</h3><p class="text-sm text-gray-500 mt-1">{{ number_format((int) $book->views) }} بازدید</p></a>@endforeach</div>@endif</section></div>
@endsection
