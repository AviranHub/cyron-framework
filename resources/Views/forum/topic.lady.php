@extends('layouts.master')
@section('content')
<div class="container mx-auto max-w-4xl px-4 py-10"><a class="text-green-600" href="{{ route('forum.index') }}">← انجمن</a><article class="bg-white dark:bg-zinc-900 rounded-lg shadow p-6 mt-5"><h1 class="text-3xl font-bold">{{ $topic->title }}</h1><small class="text-gray-500">{{ $topic->created_at }} · {{ $topic->views }} بازدید</small><div class="prose dark:prose-invert max-w-none mt-6">{{ $topic->content }}</div></article><section class="mt-6 space-y-4">@foreach($posts as $post)<div class="bg-white dark:bg-zinc-900 rounded-lg shadow p-5"><small class="text-gray-500">کاربر #{{ $post->user_id }} · {{ $post->created_at }}</small><p class="mt-3 whitespace-pre-line">{{ $post->content }}</p></div>@endforeach</section>@if(!$topic->is_locked) @if(\Cyron\Authentication\Auth::check())<form method="POST" action="{{ route('forum.reply', ['slug' => $topic->slug]) }}" class="bg-white dark:bg-zinc-900 rounded-lg shadow p-5 mt-6">@csrf<label>پاسخ شما<textarea name="content" rows="5" required class="w-full mt-2 rounded border-gray-300"></textarea></label><button class="button button--primary mt-4" type="submit">ارسال پاسخ</button></form>@else<div class="bg-white dark:bg-zinc-900 rounded-lg shadow p-5 mt-6 text-gray-600">برای پاسخ دادن ابتدا وارد حساب خود شوید. <a class="text-green-600" href="{{ route('login') }}">ورود</a></div>@endif @else<p class="text-gray-500 mt-6">این موضوع بسته شده است.</p>@endif</div>
@endsection
