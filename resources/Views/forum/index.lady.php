@extends('layouts.master')
@section('content')
<div class="container mx-auto px-4 py-10 sm:px-6 lg:px-8"><div class="mb-8 flex flex-wrap items-center justify-between gap-4"><div><span class="text-xs font-extrabold tracking-widest text-emerald-600">COMMUNITY</span><h1 class="mt-2 text-3xl font-black text-emerald-950 dark:text-white">انجمن کلبه کتاب</h1><p class="mt-2 max-w-xl leading-8 text-slate-500">جایی آرام برای پرسش، پیشنهاد و گفت‌وگو درباره کتاب‌ها.</p></div>@if(\Cyron\Authentication\Auth::check())<a class="inline-flex min-h-11 items-center justify-center rounded-xl bg-emerald-600 px-5 font-bold text-white transition hover:bg-emerald-700" href="{{ route('forum.create') }}">موضوع جدید</a>@else<a class="inline-flex min-h-11 items-center justify-center rounded-xl border border-cyan-200 bg-cyan-50 px-5 font-bold text-cyan-800 transition hover:bg-cyan-100" href="{{ route('login') }}">ورود برای ایجاد موضوع</a>@endif</div><div class="mb-8 grid gap-5 md:grid-cols-3">@if($categories->count()) @foreach($categories as $category)<a class="rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-lg dark:border-slate-800 dark:bg-slate-900" href="{{ route('forum.category', ['slug' => $category->slug]) }}"><h2 class="text-xl font-bold">{{ $category->name }}</h2><p class="mt-2 text-slate-500">{{ $category->description ?: 'گفت‌وگوهای این دسته' }}</p></a>@endforeach @else<div class="rounded-2xl border border-dashed border-slate-300 p-5 text-slate-500">هنوز دسته‌بندی‌ای ایجاد نشده است.</div>@endif</div><section class="rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="mb-4 text-xl font-bold">آخرین موضوع‌ها</h2>@if(count($topics->items())) @foreach($topics as $topic)<a class="block border-b border-slate-100 py-4 transition hover:bg-emerald-50 dark:border-slate-800 dark:hover:bg-slate-800" href="{{ route('forum.topic', ['slug' => $topic->slug]) }}"><strong>{{ $topic->title }}</strong><small class="mt-1 block text-slate-500">{{ $topic->created_at }} · {{ $topic->views }} بازدید</small></a>@endforeach @else<div class="py-6 text-slate-500">هنوز موضوعی برای نمایش وجود ندارد.</div>@endif<div class="mt-5">{!! $topics->links() !!}</div></section></div>
@endsection
